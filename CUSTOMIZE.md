# 自定义

## 用户可调（ECHO → Workshop → 已安装 → 设置）

| 设置 | 说明 |
| --- | --- |
| 打开时全屏沉浸 | 默认进入时自动最大化 ECHO 窗口，铺满窗口且不显示宿主标题栏 / 关闭按钮，用 `Esc` 退出；关闭此设置则以大面板（`full`）打开 |
| 切歌自动聚焦 | 当前曲变化时飞到最近的当前曲卡片并展开 |
| 封面灯光 | `聚光灯` 保留暗角与未选中封面阴影；`白天` 让所有封面一样亮。也可在 `Ctrl + Space` 功能面板切换 |
| 边缘暗角 / 关灯 / 非活动海报叠色 | 三项彼此独立。聚光灯模式下也可在墙上用 `V` / `L` / `T` 即时切换，会写回设置；白天只暂停生效，不改存储值 |
| 使用自定义叠色 + 自定义叠色 + 叠色强度 | 叠色默认纯黑。打开「使用自定义叠色」后才能改颜色；强度在叠色开启时生效，改颜色或强度都不会强行打开叠色 |
| 界面字体 | 功能面板可选系统默认、常见中文/西文字体，或输入字体名 |
| 格子尺寸 | S / M / L = 104 / 128 / 152 px；功能面板与 Workshop 设置页均可改 |
| 当前曲歌词 / 翻译行 | 只作用于曲库墙。展开当前曲时显示逐字歌词与翻译；也可在展开卡播放键旁切换歌词显隐 |

## 键盘

| 键 | 行为 |
| --- | --- |
| 方向键 | 在卡片间移动焦点，相机跟随 |
| Enter | 展开；已展开的当前曲 → 播放/暂停；其他曲 → 播放 |
| Space | 展开；已展开 → 显示/隐藏控制区 |
| Esc | 先收起展开卡，再按一次关闭面板 |
| C | 聚焦并展开当前曲（与右下角定位按钮相同） |
| Ctrl + Space | 打开功能面板（切墙、搜索、翻页、行为、灯光、叠色、格子、字体；曲库墙另有歌词） |
| L / V / T | 关灯 / 暗角 / 叠色（白天模式下会先切回聚光灯） |
| ← → | 进度条聚焦时 ±5 秒（进度条只在当前曲的展开卡上） |

## 专辑墙

主界面播放栏右侧的拼贴墙图标提供曲库墙、专辑墙两个入口。曲库墙只读曲库歌曲，一次最多铺 800 首，超出再翻页；
用 `Ctrl + Space` 打开功能面板：搜索歌曲、翻页、刷新，以及行为 / 灯光 / 叠色 / 格子 / 字体 / 歌词 / 切到专辑墙；播放走 `queue.playTrack`，不读取播放队列。
专辑墙每页 60 张；展开后曲目每页 50 首。控件样式与曲库墙相同：未在播这张专辑时只留靠左的「播放整张专辑」；
播到这张专辑后展开上一曲 / 暂停 / 下一曲 / 随机 / 单曲循环，并显示半宽细白进度条。
点击曲目使用宿主单曲播放。`Enter` / `Space` 在已展开的当前专辑上播放/暂停，其它专辑上 `Enter` 播放整张、`Space` 切换曲目区；焦点留在「播放整张专辑」或当前曲目上时也不会重头播放。
`C` 定位当前页内的播放专辑。打开墙面时若当前专辑在本页，会直接展开。
点开另一张未在播的专辑、或 `Esc` 收起时，正在播的那张会立刻归位成正方形，不必先回到墙面；翻页与改格子尺寸不归位。
功能面板中的 `Esc` 先返回墙面焦点，再按两段规则收起 / 关闭。曲库墙只搜歌曲，专辑墙只搜专辑。
歌词及翻译设置仅用于曲库墙；行为、灯光、叠色、字体与格子尺寸在两面墙共享。

## 改代码

1. 模板：`source/panel/geometry/blockTemplates.ts`。改完必须运行 `npm run reflows`
   重新求解让位表，然后 `npm test`（几何测试会验证 240 张表全部精确覆盖）。
2. 动效参数：`source/panel/wall/spring.ts`（弹簧刚度）、`camera.ts`（飞行时长、
   惯性衰减）、`posterWall.ts`（入场波浪延迟）。
3. 样式：`source/styles/NN-*.css`，按编号顺序拼接；主题 token 用 `--host-*` 变量。
4. 文案：`source/panel/stateOverlay.ts`（空态 / 权限态）、`wall/controls.ts`
   （按钮 aria-label）。
5. 本地预览：`npm run dev`，右上角工具栏切换夹具、明暗主题、减少动效，或模拟
   宿主切歌 / 拖动进度。

## 日常命令

```powershell
npm run reflows
npm test
npm run check
npm run dev
npm run inspect
```

发布只在 ECHO → 创意工坊 → 创作（创作台）里手动确认；本仓库不含上传脚本，
`npm run check` 等本地命令永不上传。
